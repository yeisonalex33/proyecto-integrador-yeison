from readchar import readkey, key, readchar


while True:
        print ("ingresa un tecla: ")
        caracter = readkey()
        if caracter == key. UP:
            print(f"has precionado la tecla ↑ UP!")
            break
        else:
            print(f"has presionado la tecla {caracter}, sigue intentando")